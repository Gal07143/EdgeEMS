import React from 'react';
import FDDDetection from './Detection'; // Import Detection page as default

// Default export for the /fdd route, currently showing detection
const FDD: React.FC = () => {
  return <FDDDetection />;
};

export default FDD; 