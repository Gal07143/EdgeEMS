import React from 'react';
import CarbonTracking from './CarbonTracking'; // Import existing page as default

// Default export for the /environmental route, currently showing carbon tracking
const Environmental: React.FC = () => {
  return <CarbonTracking />;
};

export default Environmental; 